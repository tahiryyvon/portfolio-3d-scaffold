generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Portfolio {
  id          Int     @id @default(autoincrement())
  title       String
  description String?
  imageUrl    String?
  projectUrl  String?
}
